import {expect} from "chai";

import Service from "./index";

describe("Services", () => {

    describe("CurrencyService", () => {

        it("new", () => {
            expect(1).to.be.equal(1);
        });

    });

});
