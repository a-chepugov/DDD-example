import {expect} from "chai";

// import {EventNumber, SubscriberEventNumber, ChannelEventNumber} from "./index";

describe("Events", () => {

    describe("_", () => {

        it("publish", () => {
            expect(1).to.be.equal(1);
        });

        it("unsubscribe", () => {
            expect(1).to.be.equal(1);
        });

    });

});
